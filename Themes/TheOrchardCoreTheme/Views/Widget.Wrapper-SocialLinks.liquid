{%- comment -%}
  Strip the default Layers .widget-container wrapper (same trick as the footer widgets) so the header
  'Header' zone emits just the social-icon row.
{%- endcomment -%}
{{ Model.Content | shape_render }}
