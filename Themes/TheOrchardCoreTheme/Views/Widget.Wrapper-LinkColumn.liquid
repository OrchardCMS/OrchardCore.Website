{%- comment -%}
  Strip the default Layers .widget-container wrapper (see Widget.Wrapper-PartnerPromo.liquid) so each link
  column is a direct grid cell of the 'Footer Bottom' row.
{%- endcomment -%}
{{ Model.Content | shape_render }}
