@*
    Strip the default Layers .widget-container wrapper (see Widget.Wrapper-PartnerPromo.cshtml) so the brand
    widget is a direct grid cell of the 'Footer Bottom' row.
*@
@await DisplayAsync(Model.Content)
