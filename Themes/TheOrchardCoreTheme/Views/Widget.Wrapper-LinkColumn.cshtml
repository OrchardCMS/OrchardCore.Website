@*
    Strip the default Layers .widget-container wrapper (see Widget.Wrapper-PartnerPromo.cshtml) so each link
    column is a direct grid cell of the 'Footer Bottom' row.
*@
@await DisplayAsync(Model.Content)
